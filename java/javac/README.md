# Java (bare javac) template

Includes:

- JDK 21 devshell
- No build tool; just `javac`/`java`
- Tiny `Makefile` for build/run/test

## Use

Build and run:

```bash
make run
```

Run a simple test:

```bash
make test
```
